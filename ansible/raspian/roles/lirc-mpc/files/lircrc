    begin
            prog = irexec
            button = KEY_POWER2
            config = mpdctrl toggleplay
    end
    begin
            prog = irexec
            button = KEY_SAVE
            config = mpdctrl resetchannels
    end
    begin
            prog = irexec
            button = KEY_ENTER
            config = mpdctrl saytitle
    end
    begin
            prog = irexec
            button = KEY_CHANNELDOWN
            config = mpdctrl channeldown
    end
    begin
            prog = irexec
            button = KEY_CHANNELUP
            config = mpdctrl channelup
    end
    begin
            prog = irexec
            button = KEY_KPPLUS
            config = mpdctrl cdmode play
    end
    begin
            prog = irexec
            button = KEY_KPMINUS
            config = mpdctrl cdmode rip
    end
